# Travel Weather Planner — 产品路线图

> 状态:草案,2026-09-08 起草,待馨洁确认优先级。目标:让这个项目从"能跑的脚本"变成"真的有人会想用"的产品,不是练手作业。

## 为什么值得做,而不是直接用现成的天气 APP

普通天气 APP 回答的问题是:"我已经决定去这个地方了,那边现在/这周天气怎样?"

它不做的事:
- 不帮你在多个候选目的地/日期之间**做决定**
- 不理解"一次旅行"是一个整体(可能横跨好几个城市、好几天),只按单个地点、单个时间点查
- 不会把天气变成**具体建议**(哪天该排室内活动、要不要带雨具、怎么打包)
- 查完就完了,不记得你的旅行、不会随着出发日期临近提醒你天气变化

**差异化定位:不是"查天气的工具",是"用天气信息帮你规划和决策一次旅行"的工具。** 这个定位下,才谈得上"为什么用这个,不用天气 APP"。

## 现状(2026-09-08)

只能算草稿:命令行脚本,一次交互式流程,创建一个 trip → 加一个地点 → 查一次天气,存本地 JSON 文件。已经具备:多 trip 持久化、地点查找、基础容错(城市查不到 / 日期超出预报范围)。

## 分层路线图

### 第一层:地基(不做,后面所有"真实用户"相关的东西都建不起来)

- **加入"用户"这个概念**——现在 `Trip`/`Location` 完全不知道"这是谁的"。这是"能不能给不止你一个人用"的前提,Day 1-3 已经反复讨论过原因(JSON 单文件在多用户场景下的局限)。
- **从 JSON 文件换成真正的数据库**(建议从 SQLite 开始——本地文件形式,不用单独装服务器,但已经是真数据库,支持并发写入、结构约束、按条件查询。这个正好和秋季 COMP1048 Databases and Interfaces 直接衔接)。
- 没有这两样,后面的功能加得越多,以后重构的成本越高——**这层应该是下一个要啃的大方向**,不是锦上添花。

### 第二层:核心差异化功能(建立"为什么用这个"的理由,不需要地基完全打完才能做实验性版本)

- **多目的地 / 多日期方案对比**——比如"我在纠结十月初去东京还是十月底去,哪个天气更适合户外活动",一次性并排显示几个候选方案的天气,而不是一次只能查一个。这是最直接体现"帮你做决定"而不是"帮你查一个已经定好的东西"的功能。
- **天气感知的行程建议**——根据预报,给出"哪天适合安排室外/室内活动"这类具体建议,而不是甩一堆数字给用户自己判断。

### 第三层:AI Agent 集成(你直接问到的这块,也是最贴近你自己想探索的 AI Product 方向)

- **自然语言旅行规划助手**——用户直接说"帮我规划一个五天的东京行程,户外活动尽量避开下雨天",Agent 结合天气数据和 trip 结构,生成建议行程、打包清单,并能继续追问式对话调整。这是普通天气 APP 完全做不到的,是最强的差异化点,也是能写进作品集、讲清楚"我做了什么、为什么这么设计"的部分。
- 技术上会用到调用 LLM API(比如 Claude API)这类新技能,可以放进 Phase 1(秋季)专业知识轨道里单独学。

### 第四层:锦上添花(优先级最低,前面没做完不用碰这些)

- 旅行日记/历史记录(旅行结束后补记录、照片)
- 出发日期临近时,天气预报变化的提醒通知
- 展示层面的小美化(比如之前聊的"trip 编号"这种)

## 建议的推进顺序

1. 第一层(用户概念 + SQLite 数据库)——这是唯一"不做后面全部要返工"的部分,而且直接对上 COMP1038/COMP1048 的课程内容,一举两得。
2. 第二层里挑"多目的地对比"先做——不需要 AI,纯逻辑,能较快看到差异化效果。
3. 第三层(AI Agent)——地基和核心功能稳定之后再做,体验才扎实,不是搭个空壳接 API。
4. 第四层——有余力再说,不追求现在做完。

## 待馨洁确认

- 这个优先顺序你同意吗?还是想调整?
- 第一层要不要现在就开始(用 SQLite 替换 JSON),还是先做一版"多目的地对比"的原型再回头补地基?两种做法都合理,取决于你更想先看到什么效果。
